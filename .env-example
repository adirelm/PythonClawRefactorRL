# Sample environment file. Copy to `.env` (gitignored) and fill in if needed.
# All keys here are placeholders — A4 has no live API integrations at bootstrap,
# but we keep the Kaggle credentials around for carryover symmetry with A3.

KAGGLE_USERNAME=
KAGGLE_KEY=

# Future expansion slots (kept empty on purpose):
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
WANDB_API_KEY=
